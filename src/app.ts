class App {
    sum(a: number, b: number): number {
        let c = a + b;
        return c;
    }

    difference(a: number, b: number): number {
        let c = a - b;
        return c;
    }
}

export default new App();
